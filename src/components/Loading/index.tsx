import { LoadingLogo } from './styled'
import Logo from '../../public/assets/Logo.svg?component'

export const Loading = () => {

    return (
        <LoadingLogo>
            <Logo />
        </LoadingLogo>
    );
};